import { StyleDictionaryProcessor } from "@/components/StyleDictionaryProcessor";

const Index = () => {
  return (
    <div className="min-h-screen bg-background">
      <StyleDictionaryProcessor />
    </div>
  );
};

export default Index;
